# 代码变更检查工作流

## 输入

- 项目目录。
- 需求、设计、任务或待办文档。
- Git、SVN、patch、目录快照或最终代码状态。
- 用户特别关注的业务风险。
- 项目隐式规则，例如内部寻址、权限、状态机、第三方对接规范。

## 步骤

1. 确认变更来源：Git 优先，其次 SVN；没有版本管理时使用目录快照。优先使用 `--interactive` 让用户用方向键、空格和回车选择本次迭代包含的提交记录。Git 也可通过 `--base-ref` 和 `--target-ref` 检查一次迭代范围，SVN 也可通过 `--svn-revision` 检查版本范围。
2. 收集需求证据：读取 OpenSpec、spec-kit、superpowers、Markdown、todo 和用户补充说明。
3. 建立需求-提交映射：交互模式下，把用户选中的每个提交关联到需求、任务或关键约束。无法关联的提交和需求都要在报告里暴露。
4. 提取代码证据：变更文件、HTTP/RPC 调用、数据库写入、配置读取、权限、状态、金额、第三方对接。
5. 生成证据包：使用 JSON 保存原始线索，使用 Markdown 保存人工阅读报告。
6. 人工复核：优先阅读 `critical` 和 `high` 命中位置。
7. 形成结论：明确是否建议合并/发布、需要补测什么、哪些位置需要回退或重做。

## 判断原则

- 先看证据，再做结论。
- 不把“运行不报错”当作质量通过。
- 不把 AI 自评当作审计结果。
- 需求、设计、任务和代码必须能互相追踪。
- 提交记录必须尽量关联到具体需求或任务；没有来源的提交要标成审计风险。
- 对旧逻辑和隐式规则保持怀疑，尤其是调用链、寻址、权限和状态流转。
